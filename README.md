# Run command

`cmake --build build && cd build && make && cd .. && ./build/my_app ./temp/bike.jpg`

# TODO

- Add progress bar
- Add testing
- Add errors to classes
- Add flags and arguments
- Split the code into multiple files
